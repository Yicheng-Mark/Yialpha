# Yialpha 下一会话交接与未完成任务

交接日期：2026-09-06。本文只交接计划，不授权真实 LLM、真实到期评分、提交或部署。

**A 阶段更新（2026-09-06）：已完成最终审阅及必要修复，同一 18 文件离线回归现为 338 passed，最终网络尝试计数 0。**
原 316 项记录是前一轮基线，以下第 1 节保留其历史上下文。
交付见 [P0_FINAL_REVIEW.md](P0_FINAL_REVIEW.md)；下一阶段具体入口、独立账本及矩阵见
[P0_SHADOW_VALIDATION_PLAN.md](P0_SHADOW_VALIDATION_PLAN.md)。保护目录 611 文件与原基线零差异。

**B0 更新（2026-09-07）：B0 离线准备已实现并验证完毕。**
`scripts/p0_shadow/`（guard/artifacts/audit/runner）与 CLI `scripts/p0_shadow_validation.py`（offline-demo / inspect）
端到端可用；实现过程中发现并修复 6 处缺陷（详见 [P0_TIME_CONTRACT.md](P0_TIME_CONTRACT.md) 2026-09-07 补充）。
20 文件离线回归 **381 passed**、网络拦截 0、Ruff/mypy 通过、保护目录未变。
下一阶段为 B1 真实形成时快照，**须用户明确授权后另行安排，本会话未启动任何真实采集或 LLM**。
后续从 B1 开始；不要再重做 A 或 B0。
本页第 7 节的 A 阶段起始指令已执行，后续不要重复从头审阅/实现；以最新审阅记录为交付状态。

## 1. 当前状态

- 项目：`C:\Users\warri\Desktop\Yialpha`。
- 已核验可用 Python：`C:\Users\warri\Desktop\Yialpha\.venv-dev\Scripts\python.exe`。
  最初交接中的 `C:\Users\warri\Desktop\Yialpha.venv-dev\Scripts\python.exe` 不存在。
- HEAD：`62b1fa02dce44a693a75603e0085dd4a351a5f8a`，未提交、推送或部署。
- P0 代码修复、模拟数据回归、契约文档已完成；新会话必须保留当前未提交修改，不能按旧交接的“工作区干净”处理。
- 本文写入前为 15 个已跟踪文件修改、5 个新增 P0 文件；本文是额外的交接文档。
- 最近一次验证：18 个测试文件共 **316 passed in 9.83s**；Ruff 通过；9 个生产模块的限定范围 mypy 通过；`git diff --check` 通过。
  这是指定范围离线回归，不能写成全仓测试或真实到期验收通过。
- 状态交接时再次核验：20 个 P0 文件与上述验证后的清单一致；round6/round6b 共 611 个文件，SHA-256 零差异。
- 原验收结果仍是交接提供的 predictions=30、evidence=21、complete_outcomes=0；本轮未在原账本计算到期结果。

详见 [P0_TIME_CONTRACT.md](P0_TIME_CONTRACT.md)，其中包含契约、已知限制、复现过程与完整离线测试命令。

## 2. 所有阶段共同约束

1. 全程 analysis_only / shadow，live_execution=false；不修改执行开关、密钥或 `.env`。
2. 不读取 `.env`。包初始化存在 dotenv 自动加载路径，执行任何会导入项目的 Python 命令前必须先禁用 dotenv。
3. 当前范围只包含 P0；P1/P2/P3 不因“继续”自动进入执行范围。
4. 不启动真实 LLM，不重跑已完成的 preflight/analyze/reconcile/scoreboard 验收。
5. 不修改 `analysis_output/shadow-glm-round6-20260905`、`analysis_output/shadow-glm-round6b-20260905` 的任何报告、脚本、账本或证据。
6. 离线测试使用模拟数据、临时目录、临时账本，并阻断网络；不要让默认 ledger/cache 路径落到真实数据。
7. 不覆盖历史 predictions/outcomes，不放宽 intraday knowability gate，不把形成后的最终日线价格冒充形成时已知价格。
8. 常规本地审查、必要修复与测试可直接推进；不自动 git 提交、推送、部署。

执行偏差已披露：最初 11 项特征复现使用普通 pytest 入口，可能触发 dotenv 隐式读取；发现后所有回归均先禁用 dotenv，未输出或修改密钥。
新会话不要重复这个入口问题。

## 3. 已完成的 P0，避免重复实现

- 新预测冻结确定性行情参考价、价格时间、可知时间、观测时间和形成时间；不接受 LLM 自报参考价。
- SQLite 内部 schema 4 增加可空 timing/scoring_context；旧行不回填、不改写。
  这与 `yialpha.versions.SCHEMA_VERSION` 的原值 `v1` 是不同版本轴。
- 新评分口径 `close_reference_v1`，旧口径 `legacy_daily_v1`；不修改原资金费符号、校准标签或多空策略收益定义。
- 固定形成时间加 1/5/21 自然日期限；使用期限前最后已闭合日线，价格与资金费统一窗口，费用只计一次。
- 无新增交易时段直接 incomplete；可补数的新样本仅在内存 pending，期限后 7 自然日仍缺数据则终止。
- 已有任意 outcome，包括旧 pending/incomplete，退出自动队列；保留幂等与冲突改写拒绝，单样本问题不阻塞后续样本。
- scoreboard 仅聚合合格 complete，验证新口径时间与原快照一致，并区分新旧版本。
- 追加修复了旧合约 entry 缺日绕过 gate，以及股票复权基准变化导致混基准收益误评分的问题。

## 4. 尚未完成的工作与推荐顺序

### A. 下一会话：P0 最终审阅与验证方案准备

范围仍是离线工作。代码与回归已经完成，这一步是审阅交付及准备真实验证，不是从头重做 P0。

1. 核验 HEAD、`git status --short`、当前 diff 和本地约定；保留所有现有修改及后来新增的用户修改。
2. 阅读契约文档与改动，集中检查以下语义是否准确披露：
   - 新口径是日线参考收益基准，窗口可能含预测形成前的部分时段，不能解释为提交后可执行策略收益。
   - 股票只使用保守工作日规则，未实现完整交易所节假日日历。
   - 股票复权基准不一致会拒绝评分，未实现公司行动自动换算。
   - 未实现历史参考价自动重建，也未提供覆盖旧结果或追加复评的入口。
3. 只有发现具体缺陷时才增加复现测试、修复和运行相关回归；没有新变更不需要反复运行已通过的同一批检查。
   如有生产代码修改，按契约文档列出的 18 个文件完成必要回归，并更新真实结果。
4. 准备真实 shadow 验证方案：独立输出目录、独立账本、数据来源、UTC 时间、快照证据、实际窗口、费用分解、缺失原因、预期状态、版本与退出条件。
5. 交付审阅结论及具体可执行方案，明确哪些后续动作仍需另行安排。不要在准备方案时启动真实验证。

完成标准：P0 diff 范围清楚、没有未解释的阻断问题；验证方案可审阅；保护目录未变化；文档如实区分模拟与真实证据。

### B. 之后另行安排：P0 真实数据与自然到期 shadow 验证

这部分尚未执行，当前计划不启动网络采集、真实评分、定时任务或 LLM。
用户明确授权某一具体阶段后可直接推进该阶段，不重复请求已经给出的授权。

1. **真实快照验证**：在独立账本创建明确标为流程验证、非 LLM 研究输出的样本，核对参考价来源及形成、可知、观测、价格时间。
   若改为真实 LLM 形成预测，必须另行安排；不能把人工/固定概率样本当作预测能力证据。
2. **自然到期验证矩阵**：覆盖 BTCUSDT、ETHUSDT、MUUSDT 合约各 1/5/21d，以及 MU UNDERLYING 周末 1d 和 5d/21d。
   起始时间取实际提交的 UTC 时间；不能用当前代码事后伪造 9/5 的新口径快照。
3. **到期归因核验**：检查闭合终点、精确日线、资金费覆盖、重复/冲突证据、费用单次计入和真实供应商数据的时间语义。
4. **生命周期核验**：覆盖同根终态、数据未齐 pending、7 天后 incomplete、重复调用幂等及队列不阻塞。
5. **汇总核验**：核对合格 complete、拒绝原因、新旧版本分桶与账本来源，生成独立报告。
6. **后续表现评估**：另行安排真实研究预测的 shadow 样本积累与校准评估；预先确定指标及样本划分。
   固定概率或人工流程样本只能验证管线，不能替代预测能力证据，也不能与真实研究样本混合宣称有效。

时间依赖：新建 21d 样本必须自然等待其真实到期；若必要数据缺失，终态验证可能延至形成后 28 天。
离线模拟时钟已经覆盖逻辑，但不能替代上述真实等待。不得静默创建后台监控或定时任务。

完成标准：真实数据支持的流程和时间契约被验证；允许明确不可评分，不以所有样本 complete 为目标。
这仍不是预测有效性、盈利性或实盘可用性验收。

### C. 后续独立范围：P1 / P2 / P3

| 阶段 | 尚未完成的任务 | 完成证据 |
|---|---|---|
| P1 | 明确 LONG/SHORT/FLAT 与开仓、加仓、减仓、平仓；统一模型、候选票据、resolver、最终票据；替换缺乏证据的空头过渡定仓；验证双侧资金费、止损、清算和组合约束 | 先形成独立版本契约与离线复现，再交付实现及两侧回归；不改 P0 校准标签定义 |
| P2 | 区分慢速研究与临近使用复核；记录快照时间、有效期及过期处理；按持有周期定义延迟预算 | 过期输入、重新取价、成本/风险变化的离线测试与可审阅流程 |
| P3 | 分析师直接汇总 / 一轮辩论 / 两轮辩论的同样本对照；控制模型、数据、配置并做样本外评估 | 预先确定指标与实验方案，再另行安排真实 LLM 实验；比较校准、方向、失败率、耗时和成本 |

此前误将“继续1”解释成开始 P1，仅进行了初步审计并新增两份复现测试，没有 P1 生产代码修改。
两份文件已经移出工作区，暂存于：
`C:\Users\warri\AppData\Local\Temp\yialpha-p1-deferred-176d28dce20d4d6d93776fc7d0a0446f`。
这不是已完成的 P1 交付；以后开始 P1 时可先审阅这些线索，不能未经核验直接套用。

## 5. 旧样本边界

- round6b 的 24 条盘中 CONTRACT 缺冻结参考快照，继续按旧 gate 不可评分；等待不会消除历史可知性问题。
- MU 两条周末 UNDERLYING 1d：固定同根窗口为明确不可评分终态，不偷偷移到周一。
- MU 其余四条 UNDERLYING 5d/21d：保留旧评分路径，是否 complete 取决于闭合行情与成本等必要数据。
- 上述是代码与离线模拟结论；原账本仍未执行这轮到期评分。
- 如未来需要研究旧数据，只能另行安排独立副本/独立输出；不能写回受保护原件。
  若希望复评已有不可变 outcome，需要另行设计带版本关联的追加复评机制，本次 P0 不包含它。

## 6. 核验线索

- 契约与测试命令：`docs/P0_TIME_CONTRACT.md`。
- P0 核心模块：`yialpha/ledger/time_contract.py`、`outcome_compute.py`、`outcomes.py`、`predictions.py`、`scoreboard.py`、`models.py`、`sqlite.py`；
  以及 `yialpha/agents/utils/prediction_tools.py`、`yialpha/versions.py`。
- 本机保护文件基线：`C:\Users\warri\AppData\Local\Temp\yialpha-p0-round6-manifest-before.json`。
- 本机 20 个已验证 P0 文件清单：`C:\Users\warri\AppData\Local\Temp\yialpha-p1-start-p0-manifest.json`。
  名称来自当时的范围误读，内容是 P0 哈希；不含本文。临时文件如已消失，应报告无法复核原基线，不能重建后宣称零差异。

## 7. 可直接复制到新会话的起始指令

```text
继续 Yialpha 的 P0 收尾。本会话先执行 docs/P0_NEXT_SESSION_PLAN.md 中 A 阶段：
核验并保留现有未提交 P0 diff，阅读 docs/P0_TIME_CONTRACT.md，进行最终审阅，
仅修复具体发现的问题，并准备可审阅的真实 shadow 到期验证方案。
P0 已有 316 项指定范围离线回归通过，不要从头重做，也不要重跑 round6/round6b 验收。
项目 C:\Users\warri\Desktop\Yialpha；Python 使用项目内 .venv-dev\Scripts\python.exe。
全程 analysis_only/shadow，live_execution=false；不读写 .env，导入项目之前禁用 dotenv。
离线测试阻断网络并使用模拟数据与临时账本；不修改原 round6/round6b 文件。
本会话不执行 B 阶段真实采集/到期评分，不启动真实 LLM，不创建自动化，不进入 P1/P2/P3，
不提交、推送或部署。最后交付审阅结论、必要 diff、验证结果与下一阶段具体计划。
```
